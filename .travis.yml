language: ruby
rvm: "2.3.1"
before_install:
  - nvm install node
  - nvm use node
  - node --version
  - ruby --version
install:
  - npm install -g webpack
  - npm install
  - bundler install
script:
  - webpack --minimize
  - bundle exec jekyll build
